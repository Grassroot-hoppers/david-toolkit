# Dashboard V2 Build Design
**Date:** 2026-03-11
**Status:** Approved
**Scope:** Tab 1 (Briefing du jour) + Tab 2 (Produits) as heroes, Tabs 3–7 as stubs. 2 days: Thursday full day (8h) + Friday 2h.

---

## Architecture

Fresh build in `public/`. The alpha in `demo/` stays untouched.

| File | Purpose |
|------|---------|
| `public/index.html` | Shell, tab nav, 8 section stubs |
| `public/styles.css` | Adapted from `demo/styles.css` — dark theme, no reinventing |
| `public/app.js` | Tab switching, Tab 1 runtime briefing logic, Tab 2 rendering |
| `public/data/demo.json` | Existing file, expanded with products + monthly history + groups |

Single fetch, no backend, no build step at runtime. The expanded demo.json is generated by `npm run build:demo`.

---

## demo.json Expansion

Three additions to `build-demo.mjs`:

### 1. `products[]` — top 150 products by 2025 revenue

Each product:
```json
{
  "name": "clean name",
  "category": "clean category",
  "supplier": "clean supplier",
  "rank": "A|B|C|D",
  "revenue2025": 0,
  "revenue2024": 0,
  "growth": 0.12,
  "growthLabel": "en hausse",
  "monthlyHistory": [0, 0, ...],
  "suggestedOrder": { "qty": 45, "basis": "Semaine 11, 2024–2025, tendance +12%" }
}
```

`monthlyHistory` = 36 values (Jan 2023 → Dec 2025) from `monthly-product-stats.json`.
`suggestedOrder` = avg of same week ±2 weeks across last 2 years × trend factor. Only computed for A and B rank products (D products are delisting candidates — no order qty shown).
Source for rank: revenue Pareto — A = top 20% of cumulative revenue, B = next 30%, C = next 30%, D = bottom 20%.

### 2. `productGroups[]` — ~10 pre-seeded groups

Defined in `sample-data/config/product-groups.json`. Seeded by AI using product name keywords (pommes, tomates, fromage, oeufs, etc.).

Each group:
```json
{
  "key": "pommes",
  "displayName": "Pommes",
  "members": ["Pomme Jonagold", "Pomme Granny Smith", "Pomme Pink Lady"],
  "aggregateRevenue2025": 0,
  "seasonality": [0, 0, ...],
  "rank": "A"
}
```

Groups are seeded in the config and computed by the pipeline at build time.

### 3. `trends.hourlyHeatmap`

Already exists in `data/gold/hourly-heatmap.json`. Add it to demo.json. Used by Tab 5 stub (Tendances) and eventually by the full Tendances tab.

Expected final demo.json size: ~1.5–2MB.

---

## Tab 1 — Briefing du jour

Runtime-generated in the browser. Reads `demo.json` + `new Date()`. No rebuild needed.

### Five elements (top to bottom):

**1. Date / context strip**
"Jeudi 12 mars · Semaine 11 · Ouverture aujourd'hui: 08h–18h"
Computed from `new Date()`. Hardcoded opening hours for now.

**2. Performance gauge**
`weeklyMetrics.weekYoY` drives color: green (>+10%), blue (-10% to +10%), red (<-10%).
Shows: last week revenue vs same week last year, MTD vs same MTD last year. Already in demo.json — this is pure display logic.

**3. This-week prediction**
`same week 2025 revenue × (1 + macro YoY trend)` → predicted range.
Display: "Prévision semaine 12: ~11 300€ · Base: 6 610€ (sem. 2025) × tendance +71%"

**4. Ordering day reminder**
`supplier-map.json` has `orderingDays` per supplier (extend the existing config).
If today is an ordering day for any supplier → show "Commandes à passer: Biofresh, Vajra".
If not → "Prochain jour de commande: [supplier] lundi".

**5. Weather block**
Existing Open-Meteo fetch, unchanged. Weather signal: if rain predicted → "Pluie prévue — historiquement +8% fréquentation les jours de pluie."

---

## Tab 2 — Produits

Four sub-components:

### A. Controls bar
- Search input (filters product name live)
- Filter pills: Tout / A / B / C / D
- Secondary filter: dropdown by category (populated from product data)

### B. Product groups (accordion, top of list)
- Groups from `productGroups[]` appear above individual products
- Each group row: name, aggregate revenue 2025, ABCD badge, 12-month seasonality mini-bar (CSS, not SVG)
- Click → expands to show member products inline

### C. Product rows
Each row:
- Clean name (bold)
- Category tag (small pill)
- ABCD badge (color-coded: A=gold, B=green, C=orange, D=red)
- Growth arrow: +12% ↑ (green) or -8% ↓ (red) or ~0% → (grey)
- Inline SVG sparkline: 80×24px, 36 monthly points, single polyline
- Suggested order (A+B only): "~45 unités · sem. 11"

No chart library. Pure SVG `<polyline>` per product.

### D. Ordering suggestion tooltip
On hover/click of the "~45 unités" badge → tooltip showing:
"Basé sur semaine 11 en 2024–2025 · moyenne: 40 unités · tendance: +12%"

---

## Tabs 3–7 — Stubs

Each stub follows the same pattern:
- Tab icon + name in nav
- Section header + one-paragraph description
- "Disponible dans la version suivante" badge
- 2–3 greyed-out preview cards (show the shape, not real data)

| Tab | Name | Preview cards |
|-----|------|---------------|
| 3 | Catégories | Revenue/shelf metric card + category tree card |
| 4 | Fournisseurs | Supplier card with ordering days + revenue |
| 5 | Tendances | Monthly revenue chart stub + hourly heatmap stub |
| 6 | Pipeline | Bronze→Silver→Gold flow diagram stub |
| 7 | Données | Data audit / category mapping table stub |

---

## Build Sequence for Thursday

1. **demo.json expansion** (1.5h) — extend `build-demo.mjs`: products[], productGroups[], hourlyHeatmap. Create `product-groups.json` config with seeded groups. Verify output.
2. **Tab framework** (45min) — `public/index.html` shell, tab nav, 8 `<section>` stubs, CSS base from alpha.
3. **Tab 1** (2.5h) — runtime briefing JS: date context, performance gauge, prediction, ordering reminder, weather.
4. **Tab 2** (3h) — products array render, ABCD + growth, sparklines, groups accordion, search/filter, ordering suggestions.
5. **Stubs polish** (30min) — preview cards for tabs 3–7.
6. **Smoke test + commit** (15min)

---

## Out of Scope (Thursday)

- Shelf allocation (Tab 3) — config exists in design, not built
- Full Fournisseurs, Tendances, Pipeline, Données (Tabs 4–7) — stubs only
- Product name corrections UI (in-browser editing)
- Ranking overrides (manual ABCD override config)
- Weather-driven ordering recommendations (noted, not built)
